<main class="container">
  <h1>Shared Todo List</h1>
  <p>Share your Todo lists with your family!</p>

  <% if user_signed_in? %>
    <%= link_to "View todos", todos_path, class: "btn btn--dark" %>
  <% else %>
    <%= link_to "Sign in", new_user_session_path, class: "btn btn--dark" %>
  <% end %>
</main>